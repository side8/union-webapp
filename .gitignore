# build output
dist/
# generated types
.astro/
# Cloudflare runtime types — generated by `wrangler types` (provides
# the `cloudflare:workers` module declaration the share route imports).
# Regenerated by the `gen:types` script that typecheck depends on; not
# committed, same treatment as .astro/.
worker-configuration.d.ts
# wrangler local state (dev runtime cache, deploy bundle staging)
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.local
.env.production
# Cloudflare dev vars (consumed by @cloudflare/vite-plugin in `npm run dev`)
.dev.vars
.dev.vars.local

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
